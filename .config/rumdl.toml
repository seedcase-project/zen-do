[global]
flavor = "quarto"
exclude = [
  # Common directories to exclude
  ".github",
  # This is auto-generated from the qmd version
  "**/README.md",
  # This has it's own structure
  "**/LICENSE.md",
  # URLs get mangled if running rumdl on this file
  "**/_badges.qmd",
]

# List style: `-`
[MD004]
style = "dash"

# Wrap list items
[MD032]
allow-lazy-continuation = false

# Reflow text
[MD013]
reflow = true
reflow-mode = "normalize"

# Fix tables and their separator row
[MD060]
enabled = true
style = "aligned-no-space"
